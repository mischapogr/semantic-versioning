plugins {
    // Auto-provisions the Java 17 toolchain when a matching JDK is not installed.
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

rootProject.name = 'hello-world'
